package com.antipistonspitting;

import net.neoforged.bus.api.IEventBus;
import net.neoforged.fml.common.Mod;

@Mod("antipistonspitting")
public class AntiPistonSpittingMod {

    public static final String MOD_ID = "antipistonspitting";

    public AntiPistonSpittingMod(IEventBus modEventBus) {
        // No setup needed - all logic is handled via Mixin
    }
}
